{
  "settings": {
    "number_of_shards": 10,
    "number_of_replicas": 1
  },

  "mappings": {
    "event": { 
      "properties": { 
        "ip":         { "type": "string"  }, 
        "device":     { "type": "string"  }, 
        "uuid":       { "type": "string"  }, 
        "userUuid":   { "type": "string"  }, 
        "type":       { "type": "string"  }, 

        "data":       { "type": "object"  },
        
        "createdAt":  { "type":  "date" },
        "updatedAt":  { "type":  "date" }
      }
    }
  },

  "warmers" : {},

  "aliases" : {"events": {}}
}
